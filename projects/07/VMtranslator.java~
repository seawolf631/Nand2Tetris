import java.util.*;
import java.lang.*;
import java.io.*;
public class VMtranslator{
    public static void main(String[] args){
	String fileName = args[0];
	Parser parsedFile = new Parser(fileName);
	CodeWriter writtenFile = new CodeWriter(fileName);
	writtenFile.close();
    }
}
